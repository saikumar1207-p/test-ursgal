trigger: none
pr: none

resources:
  pipelines:
    - pipeline: mainpipe
      source: mainpipelinetest
      trigger: true

pool:
  vmImage: 'windows-latest'
  name: 'default'
stages:
  - stage: Build
    displayName: Build and Push
    jobs:
      - job: BuildAndPushJob
        displayName: Build And Push Using template
        steps:
          - script: echo "Built and pushed"
            displayName: Build & Push Step

  - stage: Confirmation
    dependsOn: Build
    jobs:
      - job: ConfirmTrigger
        steps:
          - script: echo "ACR pipeline (acrtest) triggered after mainpipelinetest completes."
            displayName: Confirm ACR Trigger
           echo "built and pushed"            
steps:
- script: echo "ACR pipeline (acrtest) triggered after mainpipelinetest completes."

