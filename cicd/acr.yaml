resources:
  pipelines:
    - pipeline: mainpipe
      source: mainpipelinetest
      trigger: 
        branches:
        exclude:
          - feature/*
pool:
  vmImage: 'windows-latest'
  name: 'default'

steps:
- script: echo "Pipeline B triggered after Pipeline A finished".   
